from django.urls import path
from .views import *

urlpatterns = [
    # path('create/', TaskCreateViews.as_view()),
    # path('all/', TaskListView.as_view()),
    # path('details/<int:pk>', TaskItemView.as_view()),

]